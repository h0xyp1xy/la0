# Rate limit zone for la0 — include in http {} of nginx.conf:
#   include /home/ktt/Documents/la0/deploy/la0-nginx-ratelimit-zone.conf;
limit_req_zone $binary_remote_addr zone=la0_limit:10m rate=10r/s;
